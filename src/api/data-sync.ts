import useSWR from 'swr';
import request from '@/utils/request';

export const usePmList = (num:number) => {
  // const res = useSWR(`https://pokeapi.co/api/v2/pokemon/${num}`, request);

  // return res.data;
}


